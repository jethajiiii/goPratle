import './globals.css';

export const metadata = {
  title: 'Post a Requirement | SprintBrief',
  description: 'Post your project requirement and connect with verified vendors and pre-vetted specialists.',
};

const RootLayout = ({ children }) => (
  <html lang="en">
    <body>{children}</body>
  </html>
);

export default RootLayout;
